<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="Apem.Views.Shell.StatusPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    NavigationCacheMode="Disabled">

    <Grid x:Name="LayoutRoot">
        <ScrollViewer
            HorizontalScrollBarVisibility="Auto"
            VerticalScrollBarVisibility="Auto"
            HorizontalScrollMode="Auto"
            VerticalScrollMode="Auto">
            <StackPanel
                x:Name="ContentColumn"
                Padding="32"
                Spacing="24"
                HorizontalAlignment="Center"
                VerticalAlignment="Top">
                <StackPanel Spacing="8">
                    <TextBlock Text="Status" Style="{StaticResource TitleTextBlockStyle}" />
                    <TextBlock
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        Text="Live connection health for GSI and overlay updates, plus the Dota 2 setup checklist."
                        TextWrapping="WrapWholeWords" />
                </StackPanel>

                <Border
                    Padding="20"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="12">
                    <StackPanel Spacing="12">
                        <TextBlock Text="Live status" Style="{StaticResource SubtitleTextBlockStyle}" />
                        <TextBlock x:Name="GsiStatusText" />
                        <TextBlock x:Name="ConnectionStatusText" />
                        <TextBlock x:Name="LastUpdateText" />
                        <TextBlock
                            x:Name="HotkeyHintText"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            TextWrapping="WrapWholeWords" />
                    </StackPanel>
                </Border>

                <Border
                    Padding="20"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="12">
                    <StackPanel Spacing="12">
                        <TextBlock Text="Setup" Style="{StaticResource SubtitleTextBlockStyle}" />
                        <TextBlock
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            Text="Get APEM connected to Dota 2 Game State Integration."
                            TextWrapping="WrapWholeWords" />
                        <TextBlock Text="1. Install GSI config into Dota cfg/gamestate_integration" />
                        <TextBlock Text="2. Steam launch option: -gamestateintegration" />
                        <TextBlock Text="3. Dota video setting: Borderless Windowed" />
                        <TextBlock Text="4. Start a match — overlay updates automatically" />
                        <TextBlock
                            x:Name="InstallMessageText"
                            TextWrapping="WrapWholeWords" />
                        <StackPanel Orientation="Horizontal" Spacing="12">
                            <Button
                                x:Name="ReinstallButton"
                                Content="Reinstall GSI config"
                                Click="OnReinstallClick" />
                            <Button
                                x:Name="ShowOverlayButton"
                                Content="Show overlay"
                                Click="OnShowOverlayClick" />
                            <Button
                                x:Name="HideOverlayButton"
                                Content="Hide overlay"
                                Click="OnHideOverlayClick" />
                        </StackPanel>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
