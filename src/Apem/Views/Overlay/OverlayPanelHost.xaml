<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="Apem.Views.Overlay.OverlayPanelHost"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Border Style="{StaticResource HudPanelStyle}">
        <StackPanel Spacing="8">
            <TextBlock Text="{x:Bind Title, Mode=OneWay}" Style="{StaticResource HudTitleStyle}" />
            <ContentPresenter Content="{x:Bind PanelContent, Mode=OneWay}" />
        </StackPanel>
    </Border>
</UserControl>
